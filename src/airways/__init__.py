"""Reusable helpers for the Human Airways Digital Twin project.

Beginner explanation:
The `src/airways/` folder contains shared code that many scripts can use.
Instead of copying the same function into several files, we put it here once.

Simple example:
Both the Streamlit app and script 08 need proxy physics metrics. They can both
import the same function from:

    airways.proxy_physics
"""
